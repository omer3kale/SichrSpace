# ─────────────────────────────────────────────────────────────────
# SichrPlace Lombok configuration
#
# Instructs Lombok to annotate all generated methods/constructors
# with @lombok.Generated so that JaCoCo 0.8.x automatically excludes
# them from code-coverage reports (since JaCoCo 0.8.2).
#
# Without this, Lombok-generated boilerplate (getters, setters,
# builders, equals, hashCode, toString, constructors) inflates the
# uncovered instruction count and suppresses the OVERALL metric.
# ─────────────────────────────────────────────────────────────────
config.stopBubbling = true
lombok.addLombokGeneratedAnnotation = true

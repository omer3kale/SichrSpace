# ── SichrPlace VPS Environment Variables ──
# Copy this file to .env and fill in the values.
#   cp .env.example .env
# NEVER commit .env to version control.
#
# Which compose file to use:
#   PostgreSQL: docker compose --env-file .env up -d
#   MSSQL:      docker compose -f docker-compose.mssql.yml --env-file .env up -d

# ─── PostgreSQL (docker-compose.yml) ───
POSTGRES_DB=sichrplace
POSTGRES_USER=sichrplace_user
POSTGRES_PASSWORD=          # Strong random password — e.g. openssl rand -base64 32

# ─── MSSQL (docker-compose.mssql.yml) ───
SA_PASSWORD=                # SA password — ≥8 chars, upper+lower+digit+symbol
MSSQL_APP_USER=sichrplace_user
MSSQL_APP_PASSWORD=         # App user password — same complexity rules
MSSQL_PID=Developer         # Developer | Express | Standard

# ─── Spring Boot API ───
JWT_SECRET=                 # At least 32 chars — e.g. openssl rand -base64 48
CORS_ALLOWED_ORIGINS=https://sichrplace.com,https://www.sichrplace.com,https://<your-github-user>.github.io

# ─── Docker Image ───
GHCR_OWNER=your-github-user          # GitHub username or org (lowercase)
IMAGE_TAG=latest                      # or a specific SHA
